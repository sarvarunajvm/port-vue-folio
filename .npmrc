# Use public npm registry for CI/CD
# Local development can override this with user/system npmrc
registry=https://registry.npmjs.org/
strict-ssl=true
fund=false
audit=false

# Retry settings to reduce flaky CI failures
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
